import styled from 'styled-components';

// Styled a div to contain the answer buttons
export default styled.div`
  width: 100%;

  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-around;
`;