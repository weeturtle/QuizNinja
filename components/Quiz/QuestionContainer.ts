import styled from 'styled-components';

// Styles the container of the question with the answers within
export default styled.div`
  display: flex;
  flex-direction: column;
  justify-content: space-around;
  align-items: center;
`;