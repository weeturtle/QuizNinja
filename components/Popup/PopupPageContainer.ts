import styled from 'styled-components';

export default styled.div`
  height: 100vh;
  width: 100vw;

  border-radius: 0;

  position: fixed;
  top: 0;
  left: 0;
  z-index: 1;
  backdrop-filter: brightness(0.5);
`;